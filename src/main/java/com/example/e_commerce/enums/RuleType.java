package com.example.e_commerce.enums;

public enum RuleType {
    AGE, // Kullanıcının yaşı (örn: yaş < 25)
    CITY, // Kullanıcının yaşadığı şehir (örn: İstanbul)
    LOYALTY_SCORE, // Kullanıcının sadakat puanı (örn: puan > 100)
    CATEGORY, // Ürünün kategorisi (örn: "Elektronik")
    USER_ROLE // Kullanıcının rolü (örn: "PREMIUM", "ADMIN")
}
